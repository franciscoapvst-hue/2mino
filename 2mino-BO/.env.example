# Base de api-integracion. Local (dev): http://localhost:3000
# Producción: http://localhost:3000 también, pero vía túnel SSH hacia el
# VPS (ver docs/CASOS_DE_USO_BACKOFFICE.md §10.1) — nunca se apunta
# directo a un dominio público.
VITE_API_URL=http://localhost:3000
